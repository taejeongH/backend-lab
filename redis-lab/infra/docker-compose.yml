version: '3.7'
services:
    redis:
        image: redis:7.4-alpine
        command: redis-server --port 6379
        container_name : redis-lab
        ports:
          - 6379:6379
        volumes:
          - redis_volume_data:/data

volumes:
  redis_volume_data: